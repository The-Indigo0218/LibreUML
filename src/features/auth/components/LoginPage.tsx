/**
 * LoginPage — placeholder shell for Phase 1 Task 03 (Login UI).
 *
 * Task 02 sets up routing so this route exists and is reachable.
 * The full form (GitHub/Google OAuth + email+password + registration toggle)
 * is implemented in Task 03.
 */
export default function LoginPage() {
  return (
    <div className="flex h-screen w-screen items-center justify-center bg-neutral-950">
      <p className="text-sm text-neutral-400">
        Login page — Phase 1 Task 03
      </p>
    </div>
  );
}
